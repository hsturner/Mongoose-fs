# Sample mongoose program: sequences should be in ()s

let y=0:
let x=2:
(
 while (y < 4)
 (
  x=x*x;
  y=y+1;
  cout "the value of y is now ";
  cout y;
  cout "\n";
 );
 cout "the final value of x is ";
 cout x; 
 cout "\n";
)
